{
  "version": 2,
  "buildCommand": "cd admin-interface && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "admin-interface/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
