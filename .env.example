# Requirements Gathering Agent Environment Variables
# Copy this file to .env.local and fill in your actual values

# GitHub Token (fine-grained personal access token with models:read permission)
# Get from: https://github.com/settings/personal-access-tokens/new
# Required scopes: models:read
GITHUB_TOKEN=your_github_token_here

# Alternative: Azure AI credentials (if using Azure instead of GitHub Models)
# AZURE_AI_API_KEY=your_azure_ai_key_here
# AZURE_AI_ENDPOINT=your_azure_ai_endpoint_here

# Model configuration (GitHub Models)
AZURE_AI_API_KEY=your_github_token_here
AZURE_AI_ENDPOINT=https://models.github.ai/inference

# Model selection (recommended: gpt-4o for higher token limits)
REQUIREMENTS_AGENT_MODEL=gpt-4o

# Optional: Custom endpoint and token overrides
# REQUIREMENTS_AGENT_ENDPOINT=https://models.github.ai/inference
# REQUIREMENTS_AGENT_TOKEN=your_token_here
